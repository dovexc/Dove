services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: dove
      POSTGRES_USER: dove
      POSTGRES_PASSWORD: dove_dev_password
    ports:
      - "5432:5432"
    volumes:
      - dove_pg_data:/var/lib/postgresql/data

volumes:
  dove_pg_data:
